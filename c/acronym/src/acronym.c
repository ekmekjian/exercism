#include <stdlib>
#include <string>

char * acronymGen(char * str)
{
    int len = strlen(str)
    char * i;
    char strin[len];
    for(i = str;*i;i++)
    {
        strcat(strin,i);
    }
    return acronym;
}

